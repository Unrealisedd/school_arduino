pull_request_rules:
  - name: botacct-impersonation-test
    conditions:
      - -merged
    actions:
      comment:
        message: "impersonation probe — posted via bot_account"
        bot_account: Unrealisedd2
