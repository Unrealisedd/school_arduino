queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions: []
pull_request_rules:
  - name: queueit
    conditions:
      - base=main
    actions:
      queue:
        name: default
